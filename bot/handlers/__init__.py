# Handlers package
# Command handlers are pure functions that take input and return text.
# They don't know about Telegram — same function works from --test mode,
# unit tests, or the real Telegram bot.
